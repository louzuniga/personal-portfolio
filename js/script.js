'use strict';

$(document).ready(() => {
  const typed = new Typed('.typed', {
    strings: ['Software Engineer', 'Student', 'Enternal Learner'],
    typeSpeed: 75,
    loop: true,
    startDelay: 1000,
    showCursor: false
  });
});
